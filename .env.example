# Dev default: proxy lewat Vite agar tidak kena CORS.
VITE_API_URL=/api

# Optional: ganti target backend untuk proxy dev.
VITE_API_PROXY_TARGET=http://127.0.0.1:8000

# Optional: pakai direct backend API jika CORS backend sudah dikonfigurasi.
# VITE_API_URL=http://127.0.0.1:8000/api
